#!/bin/sh
# Bloquea push si fallan comprobaciones del sitio.
# Instalación (una vez por clon): npm run hooks:install
set -e
cd "$(git rev-parse --show-toplevel)" 2>/dev/null || exit 0
exec npm run verify
